# AGENTS.md

## プロジェクトの目的

- Linux初心者が画面で設定を選び、安全なサーバー初期設定手順を生成できるWebアプリを作る。
- 対象OSは Ubuntu Server 24.04 LTS のみとする。
- 生成物は、利用者が内容を理解しながら手動実行できる安全な手順書である。
- UI、説明、警告、エラーは日本語で書く。コマンド、パス、識別子は原文を保つ。

## 優先順位

判断が競合するときは、次の順で優先する。

1. SSHロックアウト、権限喪失、情報漏洩、設定破損を防ぐ。
2. 生成結果の正確性、決定性、再現性を守る。
3. 初心者が目的、影響、確認方法、復旧方法を理解できるようにする。
4. アクセシビリティと操作の明瞭さを守る。
5. 機能追加や実装上の利便性を追求する。

## MVPの範囲

- sudoを利用できる管理ユーザーの作成。
- SSH公開鍵認証とsshdの安全化。
- UFWファイアウォールの設定。
- unattended-upgradesによる自動セキュリティ更新。
- 安全な推奨値を初期選択し、変更項目は必要最小限にする。
- 各項目に推奨理由、影響、制約を短く表示する。

## 非目標

- SSH接続、サーバー検出、コマンドの自動実行は行わない。
- クラウドVM、Docker、Webサーバー、DBを構築しない。
- Ubuntu 24.04 LTS以外やデスクトップLinuxには対応しない。
- 既存サーバーの設定解析、移行、修復は行わない。
- パスワード、秘密鍵、APIキーを入力、生成、保存しない。
- 任意コマンド、生の設定ファイル、自由形式スクリプトを入力させない。
- 全処理を一括実行するシェルスクリプトを生成しない。
- 生成AIでコマンドや設定を自由生成しない。

## 技術方針

- Node.js 24 LTS、npm、React、Vite、TypeScriptを使用する。
- TypeScriptの `strict` を有効にする。
- ESLintとPrettierを使用し、lockfileをコミットする。
- 静的サイト兼PWAとしてビルドし、バックエンド、DB、アカウントを持たない。
- 外部API、解析、広告、テレメトリ、CDN、外部フォントを実行時に使わない。
- 初回取得後はオフラインで全機能を利用可能にする。
- 入力値と生成結果を端末外へ送信しない。
- Chrome、Edge、Firefox、Safariの現行版と1世代前を対象にする。
- PWAをインストールできない環境でも通常のWebアプリとして動作させる。

## アーキテクチャ

- 生成エンジンをReact、DOM、ブラウザAPIから分離した純粋TypeScriptにする。
- 処理順は「入力スキーマ → 正規化・検証 → 安全性ルール → 中間モデル → 出力」とする。
- 同じ正規化済み入力からは、常に同じ順序と内容の出力を返す。
- UIコンポーネント内にコマンド文字列や設定テンプレートを埋め込まない。
- OS知識、検証規則、テンプレート、レンダラーを責務ごとに分離する。
- 外部状態、現在時刻、乱数に依存する生成処理を作らない。
- 設定JSONにはスキーマバージョンを含め、読み込み時に検証する。

## 安全性ルール

- 入力は用途別スキーマと許可リストで検証し、検証不能な値は拒否する。
- ユーザー入力をシェルや設定ファイルへ直接補間しない。
- ユーザー名、SSH公開鍵、ポート、タイムゾーン等に専用レンダラーを使う。
- 危険な組み合わせは警告だけで通さず、生成を停止する。
- 公開鍵が有効でなければ、パスワード認証を無効化させない。
- sudo可能な管理ユーザーがなければ、rootログインを無効化させない。
- SSHポート変更時は、新ポートのUFW許可を同じ計画に含める。
- `sshd -t` による検証を、SSH設定の再読み込みより前に置く。
- 現在のSSH接続を維持し、別セッションで確認する手順を必ず示す。
- 破壊的操作、広すぎる権限、不要な `sudo`、検証不能なパイプ実行を避ける。

## 生成結果の要件

- 手順を依存関係どおりに並べ、各手順の目的を説明する。
- コマンドは手順単位でコピー可能にする。
- 設定ファイルは全文、配置先、所有者、権限を示す。
- 変更前のバックアップ手順を示す。
- 各変更に成功確認コマンドと期待結果を示す。
- 失敗時の復旧またはロールバック手順を示す。
- SSH切断やアクセス不能の可能性を、実行前に明示する。
- 画面表示に加え、Markdown手順書と個別設定ファイルをZIPで出力する。
- 入力設定は、利用者の明示操作でバージョン付きJSONとして入出力する。
- localStorage等へ自動保存しない。

## UI要件

- 「前提確認 → 管理ユーザー → SSH → UFW → 自動更新 → 最終確認 → 生成」の順で構成する。
- 最終確認画面から各入力ステップへ戻って修正できるようにする。
- 危険度や状態を色だけで表現せず、文章とアイコンを併用する。
- WCAG 2.2 AAを目標とし、キーボード操作、ラベル、フォーカス、エラー要約を実装する。
- モバイルでも手順の閲覧、コピー、ダウンロードができるようにする。
- 高度な自由入力欄を追加せず、必要なら新しい型付き選択肢として設計する。

## 根拠と変更管理

- コマンドと設定はUbuntu 24.04公式文書またはmanページを根拠にする。
- 各ルールに参照URL、対象バージョン、最終確認日を追跡可能な形で保持する。
- ブログや回答サイトだけを根拠に仕様を追加しない。
- Ubuntuの仕様変更を取り込む際は、根拠と生成物の差分をレビューする。
- MVPの範囲や安全性ルールを変える場合は、実装前に利用者の承認を得る。

## テストと完了条件

- Vitestで入力検証、正規化、安全性ルール、生成処理を単体テストする。
- React Testing Libraryでウィザード、バリデーション、エラー表示をテストする。
- Playwrightで主要正常系とSSHロックアウト防止をE2Eテストする。
- 代表入力の生成物をスナップショットで固定し、変更は意図を確認して更新する。
- 生成するシェル断片はShellCheckに通す。
- 設定構文とコマンドはUbuntu 24.04相当の隔離環境で検証する。
- バグ修正には、修正前に失敗する回帰テストを追加する。
- 完了前にformat、lint、型チェック、単体、UI、E2E、ビルドを実行する。
- 実行できない検証があれば、省略せず理由と残るリスクを報告する。
- 安全性、決定性、オフライン動作を損なう変更は完了扱いにしない。

## エージェントの作業規律

- 変更前に関連コード、テスト、公式根拠を確認する。
- 最小限の変更に留め、無関係なリファクタリングや依存追加を避ける。
- 新しい依存関係には必要性、オフライン性、保守性、ライセンスを確認する。
- エラーを握りつぶさず、初心者が次の行動を判断できる日本語で表示する。
- セキュリティ上の前提を推測で緩和しない。不明なら安全側で生成を停止する。
- READMEや仕様と挙動が変わる場合は、同じ変更で文書も更新する。
